version: '3.8'
services:
  # Infrastructure services
  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    restart: unless-stopped
    volumes:
      - ollama_data:/root/.ollama

  mongo:
    image: mongo:latest
    restart: unless-stopped
    ports:
      - 27017:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
    volumes:
      - mongo_data:/data/db

  # Application services
  ai-service:
    build: 
      context: ./ai-service
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
      - CHROMA_STORE_PATH=./chroma_store
      - LLM_MODEL=gemma3
      - MAX_CONTEXT_LENGTH=12000
    volumes:
      - ai_chroma_data:/app/chroma_store
    depends_on:
      - ollama
    restart: unless-stopped

  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - RUST_LOG=info
  #     - APP_PORT=8080
  #     - MONGO_URI=mongodb://root:example@mongo:27017
  #     - MONGO_DB_NAME=code_explorer
  #     - AI_SERVICE_URL=http://ai-service:8000
  #     - FRONTEND_API_URL=http://frontend
  #   depends_on:
  #     - mongo
  #     - ai-service
  #   restart: unless-stopped

  # frontend:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   ports:
  #     - "3000:80"
  #   depends_on:
  #     - backend
  #   restart: unless-stopped

volumes:
  ollama_data:
  mongo_data:
  ai_chroma_data:
